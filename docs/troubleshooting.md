# Troubleshooting

Run validation from the project root with `npm test`. Check the command output
before changing configuration. Never include credentials or private endpoints
in issue reports or documentation.
