# Requirements

- The derived project uses native ESM and Node.js 26 or newer.
- The derived project documents installation, usage, configuration, and
  validation for its actual behavior.
- The derived project keeps secrets out of source control and uses
  `.env.example` for required user-supplied variables.
