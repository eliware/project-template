# Examples

The [example entrypoint](example.mjs) demonstrates the starter project.

## Prerequisites

Use Node.js 26 and install dependencies with `npm ci`.

## Command

```bash
node examples/example.mjs
```

## Expected result

The example runs the starter workflow without requiring credentials or changing
external state.
